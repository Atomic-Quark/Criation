# Keep a single hoisted node_modules at the repo root so Metro, Next.js and
# the TypeScript server all resolve one copy of React and React Native.
install-strategy=hoisted
save-exact=false
fund=false
audit=false
